import pandas as pd
from app.data.provider import MarketDataProvider

class CSVMarketDataProvider(MarketDataProvider):
    def history(self, symbol: str, start: str = "", end: str = "", interval: str = "1d") -> pd.DataFrame:
        raise NotImplementedError("Use load_csv(path) for the CSV provider.")

    def load_csv(self, path: str, symbol: str = "UNKNOWN") -> pd.DataFrame:
        df = pd.read_csv(path)
        df = self.validate(df)
        df["symbol"] = symbol
        return df
