import pandas as pd

def trading_days(start: str, end: str) -> pd.DatetimeIndex:
    """
    Weekday calendar used only as a lightweight development fallback.
    Production should use an exchange holiday calendar.
    """
    return pd.bdate_range(start=start, end=end)
